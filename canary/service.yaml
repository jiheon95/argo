apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo
spec:
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
    nodePort: 30081
  type: NodePort
  selector:
    app: rollouts-demo
